package via.android.maria.first.easypay.repository.add.user.with.account;

import via.android.maria.first.easypay.model.User;

public interface AddUserWithAccountRepository {
    void addUserWithAccount(User user);
}
